// Ternary operator

// Ṣyntax condition ? (if it is true) : (if it is false)


//  Unary => when it has one operand
let x = "string";
// operator operand
console.log(typeof x);

// binary => when it has two operand
let y = 1 + 3;
//  1     2

// Ternary => it has three arguments
// 1st one is condition 2nd if it is true 3rs if false
console.log( 2 > 3 ? "true" : "false");
//operand      1        2         3

function compare(a,b){
    console.log(a > b ? '${a} is greater' : '${b} is greater');
}
compare(1,2);








